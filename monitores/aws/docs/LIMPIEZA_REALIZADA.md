# Limpieza y reorganización

Se excluyeron del entregable porque no son necesarios para operar o mantener el bot:
- `build/` y `dist/` de PyInstaller.
- `__pycache__/` y archivos `.pyc`.
- `Antiguo script/`.
- `main.spec`, acceso directo `.lnk` y HTML estático de maqueta.
- Reportes históricos y archivos temporales `~$...xlsx`.

La versión original tenía cerca de 72 MB. El proyecto fuente optimizado queda preparado para integrarse posteriormente dentro de un repositorio general de monitoreos junto con Hércules.
